name: cmake_macos_15

on:
  push:
    branches:
      - 'ros-workflows'
  pull_request:
    branches:
      - 'ros-workflows'
  workflow_dispatch:
    branches:
      - '*'

jobs:
  macos-15:
    name: macos-15
    uses: ./.github/workflows/cmake_macos_template.yml
    with:
      os: macos-15
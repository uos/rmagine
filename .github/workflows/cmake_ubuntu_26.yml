name: cmake_ubuntu_26

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:
    branches:
      - '*'

jobs:
  ubuntu-26:
    name: ubuntu-26
    uses: ./.github/workflows/cmake_ubuntu_template.yml
    with:
      os: ubuntu-26.04

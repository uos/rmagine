name: cmake_macos_14

on:
  push:
    branches:
      - 'ros-workflows'
  pull_request:
    branches:
      - 'ros-workflows'
  workflow_dispatch:
    branches:
      - '*'

jobs:
  macos-14:
    name: macos-14
    uses: ./.github/workflows/cmake_macos_template.yml
    with:
      os: macos-14